const BASE_URL = 'http://localhost:3001';

const USER = {
  name: '陈小武',
  nickname: '小武',
  teacherId: 2
};

const BADGE = {
  approval: 5,
  workbench: 5
};

module.exports = { BASE_URL, USER, BADGE };
